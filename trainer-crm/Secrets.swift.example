// Copy this file to Secrets.swift and fill in your values.
// Secrets.swift is listed in .gitignore — never commit the real file.
enum Secrets {
    // 64-char hex string — must match CLIENT_TOKEN_SECRET on the server
    static let clientTokenSecret = ""
}
